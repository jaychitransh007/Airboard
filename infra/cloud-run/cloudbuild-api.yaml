steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file=infra/cloud-run/api.Dockerfile
      - --tag=${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
