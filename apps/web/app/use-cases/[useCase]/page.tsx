import { FeatureGrid, MarketingPage } from "../../../src/features/product/PublicShell";
export default async function UseCase({params}:{params:Promise<{useCase:string}>}) { const {useCase}=await params; const name=useCase.replaceAll("-"," ").replace(/\b\w/g,(letter)=>letter.toUpperCase()); return <MarketingPage eyebrow="Airboard use case" title={`Airboard for ${name}`} summary="Keep attention on the explanation while Airboard turns intent into a clear, editable visual layer."><FeatureGrid items={[{title:"Begin from intent",description:"Say or type the change you need instead of navigating a dense diagramming tool."},{title:"Stay in context",description:"Use the standalone workspace, a live screen underlay, or a verified meeting overlay."},{title:"Leave an artifact",description:"Save, version, export and control retention after the conversation."}]} /></MarketingPage>; }
