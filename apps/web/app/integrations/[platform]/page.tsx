import { notFound } from "next/navigation";
import { FeatureGrid, MarketingPage } from "../../../src/features/product/PublicShell";
const INTEGRATIONS: Record<string,{name:string;summary:string;items:{title:string;description:string}[]}> = { "google-meet": { name: "Airboard for Google Meet", summary: "A private offscreen Airboard engine composites neon diagram ink into your outgoing Meet camera. Your audience never manages a second canvas.", items: [{ title:"Install and link",description:"Install the Chrome extension, sign in on Airboard and exchange a one-time link for a scoped installation token."},{ title:"Confirm once",description:"Review camera, microphone and voice processing before first capture. Enabled defaults apply after confirmation."},{ title:"Verify output",description:"Preflight checks the engine, camera, overlay, sender attachment, encoded frames and outbound bytes."}]}, zoom: {name:"Airboard for Zoom",summary:"Planned around Zoom Camera Mode and Zoom’s supported in-client application lifecycle.",items:[{title:"Current status",description:"Controlled-beta implementation and marketplace review are required."},{title:"Intended experience",description:"Use Airboard content in the outgoing camera without a virtual camera driver."}]}, "microsoft-teams": {name:"Airboard for Microsoft Teams",summary:"A shared-stage design-partner experience is planned first; camera overlay is not yet a supported claim.",items:[{title:"Current status",description:"Client and role feasibility must be validated on Windows, macOS and the web."},{title:"Fallback",description:"The standalone web or desktop Airboard remains available alongside Teams."}]} };
export default async function IntegrationPage({params}:{params:Promise<{platform:string}>}) { const {platform}=await params; const item=INTEGRATIONS[platform]; if(!item) notFound(); return <MarketingPage eyebrow="Integration" title={item.name} summary={item.summary}><FeatureGrid items={item.items} /></MarketingPage>; }
